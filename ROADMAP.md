# Roadmap

## 0.1 — Pi routing foundation

- [x] Resolve the current Pi CLI entrypoint before npm shims.
- [x] Route gstack `Agent` and `Task` through child Pi.
- [x] Add explicit review and specialist tools.
- [x] Block direct model CLI calls.
- [x] Add smoke diagnostics and read-only child policy.

## 0.2 — gstack workflow migration

- [ ] Replace direct model-CLI snippets in generated gstack workflows with bridge tool calls upstream.
- [ ] Add structured finding output and review evidence normalization.
- [ ] Add native `pi-subagents` integration when its public invocation API is available.
- [ ] Add cancellation and concurrent-run integration fixtures.
